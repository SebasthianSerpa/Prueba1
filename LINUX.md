Instalación Linux (Rocky Linux)

Creación de la máquina virtual
8GB RAM, 30GB disco, 2 CPU

Instalación
Se utilizó versión minimal (CLI)

Configuración de red

Comando:
ip a

Instalación de SSH

sudo dnf install openssh-server -y

Activación del servicio

sudo systemctl start sshd

Evidencias

(agregar imágenes)
